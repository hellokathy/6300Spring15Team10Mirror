package edu.gatech.seclass.prj1;

import java.io.*;

public class Main {
	public static void main(String[] args) {
		AvgSentenceLength asl = new AvgSentenceLength();
		/*
		 * FileInputStream inFile = new FileInputStream(args[0]);
		 * int cnt = 0;
		 * char letter;
		 * String Doc = "";
		 * while((i=inFile.read())!=-1) {
		 * letter = (char)cnt;
		 * Doc += letter;
		 * 
		 * asl.setDoc(Doc); // write setter to separate into sentences split(".|\\!|\\?)
		 * 			have multiple of this to compensate for possible command line args (commas etc)
		 * asl.findAvgSentenceLength();
		 */
	}

}
